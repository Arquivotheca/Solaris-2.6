/*
 * Copyright (c) 1991 Sun Microsystems, Inc.  All Rights Reserved. Sun
 * considers its source code as an unpublished, proprietary trade secret, and
 * it is available only under strict license provisions.  This copyright
 * notice is placed here only to protect Sun in the event the source is
 * deemed a published work.  Dissassembly, decompilation, or other means of
 * reducing the object code to human readable form is prohibited by the
 * license agreement under which this code is provided to the user or company
 * in possession of this copy.
 *
 * RESTRICTED RIGHTS LEGEND: Use, duplication, or disclosure by the Government
 * is subject to restrictions as set forth in subparagraph (c)(1)(ii) of the
 * Rights in Technical Data and Computer Software clause at DFARS 52.227-7013
 * and in similar clauses in the FAR and NASA FAR Supplement.
 */

#ifndef lint
#ident	"@(#)Cmd_ui.c 1.3 92/12/14"
#endif

/*
 * Cmd_ui.c - User interface object initialization functions.
 * This file was generated by `gxv' from `Cmd.G'.
 * DO NOT EDIT BY HAND.
 */

#include <stdio.h>
#include <sys/param.h>
#include <sys/types.h>
#include <xview/xview.h>
#include <xview/canvas.h>
#include <xview/panel.h>
#include <xview/scrollbar.h>
#include <xview/svrimage.h>
#include <xview/termsw.h>
#include <xview/text.h>
#include <xview/tty.h>
#include <xview/xv_xrect.h>
#include "Cmd_ui.h"

/*
 * Initialize an instance of object `CmdWin'.
 */
Cmd_CmdWin_objects *
Cmd_CmdWin_objects_initialize(Cmd_CmdWin_objects *ip, Xv_opaque owner)
{
	if (!ip && !(ip = (Cmd_CmdWin_objects *) calloc(1, sizeof (Cmd_CmdWin_objects))))
		return (Cmd_CmdWin_objects *) NULL;
	if (!ip->CmdWin)
		ip->CmdWin = Cmd_CmdWin_CmdWin_create(ip, owner);
	if (!ip->Term)
		ip->Term = Cmd_CmdWin_Term_create(ip, ip->CmdWin);
	return ip;
}

/*
 * Create object `CmdWin' in the specified instance.
 */
Xv_opaque
Cmd_CmdWin_CmdWin_create(Cmd_CmdWin_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME_CMD,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 587,
		XV_HEIGHT, 294,
		WIN_ROWS, 24,
		WIN_COLUMNS, 80,
		XV_LABEL, dgettext("SUNW_INSTALL_SWM", "Software Manager:  Command Input/Output"),
		XV_SHOW, FALSE,
		FRAME_SHOW_FOOTER, FALSE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		FRAME_CMD_PUSHPIN_IN, TRUE,
		NULL);
	xv_set(xv_get(obj, FRAME_CMD_PANEL), WIN_SHOW, FALSE, NULL);
	return obj;
}

/*
 * Create object `Term' in the specified instance.
 */
Xv_opaque
Cmd_CmdWin_Term_create(Cmd_CmdWin_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, TERMSW,
		TTY_ARGV, TTY_ARGV_DO_NOT_FORK,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 0,
		XV_Y, 0,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		OPENWIN_SHOW_BORDERS, FALSE,
		NULL);
	return obj;
}

