/*
 * Copyright (c) 1991 Sun Microsystems, Inc.  All Rights Reserved. Sun
 * considers its source code as an unpublished, proprietary trade secret, and
 * it is available only under strict license provisions.  This copyright
 * notice is placed here only to protect Sun in the event the source is
 * deemed a published work.  Dissassembly, decompilation, or other means of
 * reducing the object code to human readable form is prohibited by the
 * license agreement under which this code is provided to the user or company
 * in possession of this copy.
 *
 * RESTRICTED RIGHTS LEGEND: Use, duplication, or disclosure by the Government
 * is subject to restrictions as set forth in subparagraph (c)(1)(ii) of the
 * Rights in Technical Data and Computer Software clause at DFARS 52.227-7013
 * and in similar clauses in the FAR and NASA FAR Supplement.
 */

#ifndef lint
#ident	"@(#)File_ui.c 1.3 92/12/14"
#endif

/*
 * File_ui.c - User interface object initialization functions.
 * This file was generated by `gxv' from `File.G'.
 * DO NOT EDIT BY HAND.
 */

#include <stdio.h>
#include <sys/param.h>
#include <sys/types.h>
#include <xview/xview.h>
#include <xview/canvas.h>
#include <xview/panel.h>
#include <xview/scrollbar.h>
#include <xview/svrimage.h>
#include <xview/termsw.h>
#include <xview/text.h>
#include <xview/tty.h>
#include <xview/xv_xrect.h>
#include <group.h>
#include "File_ui.h"

/*
 * Initialize an instance of object `FileWin'.
 */
File_FileWin_objects *
File_FileWin_objects_initialize(File_FileWin_objects *ip, Xv_opaque owner)
{
	if (!ip && !(ip = (File_FileWin_objects *) calloc(1, sizeof (File_FileWin_objects))))
		return (File_FileWin_objects *) NULL;
	if (!ip->FileWin)
		ip->FileWin = File_FileWin_FileWin_create(ip, owner);
	if (!ip->FileCtrl)
		ip->FileCtrl = File_FileWin_FileCtrl_create(ip, ip->FileWin);
	if (!ip->FileDir)
		ip->FileDir = File_FileWin_FileDir_create(ip, ip->FileCtrl);
	if (!ip->FileName)
		ip->FileName = File_FileWin_FileName_create(ip, ip->FileCtrl);
	if (!ip->FileGrp)
		ip->FileGrp = File_FileWin_FileGrp_create(ip, ip->FileCtrl);
	if (!ip->FileDoit)
		ip->FileDoit = File_FileWin_FileDoit_create(ip, ip->FileCtrl);
	if (!ip->FileButtons)
		ip->FileButtons = File_FileWin_FileButtons_create(ip, ip->FileCtrl);
	window_fit(ip->FileCtrl);
	
	window_fit(ip->FileWin);
	
	xv_set(ip->FileCtrl,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		NULL);
	return ip;
}

/*
 * Create object `FileWin' in the specified instance.
 */
Xv_opaque
File_FileWin_FileWin_create(File_FileWin_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME_CMD,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 394,
		XV_HEIGHT, 115,
		XV_LABEL, dgettext("SUNW_INSTALL_SWM", "Software Manager:  File Chooser"),
		XV_SHOW, FALSE,
		FRAME_SHOW_FOOTER, TRUE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		FRAME_CMD_PUSHPIN_IN, FALSE,
		NULL);
	xv_set(xv_get(obj, FRAME_CMD_PANEL), WIN_SHOW, FALSE, NULL);
	return obj;
}

/*
 * Create object `FileCtrl' in the specified instance.
 */
Xv_opaque
File_FileWin_FileCtrl_create(File_FileWin_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "swmtool:FileCtrl",
		XV_X, 0,
		XV_Y, 0,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		WIN_BORDER, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `FileDir' in the specified instance.
 */
Xv_opaque
File_FileWin_FileDir_create(File_FileWin_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "swmtool:FileDir",
		XV_X, 30,
		XV_Y, 15,
		PANEL_VALUE_DISPLAY_LENGTH, 32,
		PANEL_VALUE_STORED_LENGTH, 1024,
		PANEL_LABEL_STRING, dgettext("SUNW_INSTALL_SWM", "Directory:"),
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `FileName' in the specified instance.
 */
Xv_opaque
File_FileWin_FileName_create(File_FileWin_objects *ip, Xv_opaque owner)
{
	extern Panel_setting	File_FileWin_FileName_notify_callback(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "swmtool:FileName",
		XV_X, 54,
		XV_Y, 45,
		PANEL_VALUE_DISPLAY_LENGTH, 32,
		PANEL_VALUE_STORED_LENGTH, 1024,
		PANEL_LABEL_STRING, dgettext("SUNW_INSTALL_SWM", "Name:"),
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, FALSE,
		PANEL_NOTIFY_PROC, File_FileWin_FileName_notify_callback,
		NULL);
	return obj;
}

/*
 * Create object `FileGrp' in the specified instance.
 */
Xv_opaque
File_FileWin_FileGrp_create(File_FileWin_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, GROUP,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 30,
		XV_Y, 15,
		GROUP_TYPE, GROUP_COLUMN,
		GROUP_MEMBERS,
			ip->FileDir,
			ip->FileName,
			NULL,
		GROUP_COLUMN_ALIGNMENT, GROUP_LABELS,
		GROUP_VERTICAL_SPACING, 15,
		GROUP_ANCHOR_OBJ, ip->FileCtrl,
		GROUP_ANCHOR_POINT, GROUP_NORTHWEST,
		GROUP_REFERENCE_POINT, GROUP_NORTHWEST,
		GROUP_HORIZONTAL_OFFSET, 30,
		GROUP_VERTICAL_OFFSET, 15,
		NULL);
	return obj;
}

/*
 * Create object `FileDoit' in the specified instance.
 */
Xv_opaque
File_FileWin_FileDoit_create(File_FileWin_objects *ip, Xv_opaque owner)
{
	extern void		File_FileWin_FileDoit_notify_callback(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "swmtool:FileDoit",
		XV_X, 172,
		XV_Y, 90,
		PANEL_LABEL_STRING, dgettext("SUNW_INSTALL_SWM", "Save"),
		PANEL_NOTIFY_PROC, File_FileWin_FileDoit_notify_callback,
		NULL);
	return obj;
}

/*
 * Create object `FileButtons' in the specified instance.
 */
Xv_opaque
File_FileWin_FileButtons_create(File_FileWin_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, GROUP,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 172,
		XV_Y, 90,
		GROUP_TYPE, GROUP_NONE,
		GROUP_MEMBERS,
			ip->FileDoit,
			NULL,
		GROUP_ANCHOR_OBJ, ip->FileGrp,
		GROUP_ANCHOR_POINT, GROUP_SOUTH,
		GROUP_REFERENCE_POINT, GROUP_NORTH,
		GROUP_HORIZONTAL_OFFSET, 0,
		GROUP_VERTICAL_OFFSET, 30,
		NULL);
	return obj;
}

