#include <sys/param.h>
#include <sys/time.h>
#include <sys/types.h>
#include <sys/proc.h>

proc
,#(<f-<e)$<
<f>t
<f+{SIZEOF}>f
,#{*p_stat,<t}$<traceall.nxt
{*p_pidp,<t}$<<pid.print
<t$p
,#({*p_flag,<t}&1)$<traceall.nxt
$c
$<traceall.nxt
